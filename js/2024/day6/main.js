
export function part1(input) {
    input = input.split('\n')
    const rows = input.length;
    const cols = input[0].length;

    const directions = [
        [-1, 0],
        [0, 1],
        [1, 0],
        [0, -1],
    ];
}